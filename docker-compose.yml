services:
  consul:
    image: hashicorp/consul:1.22.5
    command:
      - agent
      - -dev
      - -client=0.0.0.0
      - -ui
    ports:
      - "8500:8500"
      - "8600:8600/udp"
    healthcheck:
      test: ["CMD", "consul", "members"]
      interval: 5s
      timeout: 3s
      retries: 12
